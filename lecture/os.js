const os = require("os");
// 모듈을 require 로 가져옴 내장모듈이라 경로말고 이름만 적어줘도됨
// 운영체제에 대한 정보를 담고있음

// 이런식으로 cpu에 대한 정보도 볼수있지렁
console.log(os.cpus());

// https://nodejs.org/dist/latest-v19.x/docs/api/
// 여기서 노드가 제공하는 기능들을 볼수있지렁
